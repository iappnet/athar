import 'package:flutter/material.dart';

class DhikrPage extends StatelessWidget {
  const DhikrPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Dhikr Page')),
    );
  }
}
